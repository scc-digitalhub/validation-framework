import { Card, CardHeader, CardContent } from '@material-ui/core';

const dashboard = () => (
    <Card>
        <CardHeader title="Validation UI" />
        <CardContent>Welcome to the UI for the validation framework.</CardContent>
    </Card>
);

export default dashboard;